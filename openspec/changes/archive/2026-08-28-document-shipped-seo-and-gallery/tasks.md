## 1. Write the specs

- [x] 1.1 `seo-and-crawlability`, read from `src/data/seo.ts`, `src/components/Head.tsx`, `vite.config.ts`, `scripts/prerender.mjs`, `netlify.toml`, `public/robots.txt`
- [x] 1.2 `gallery`, read from `src/pages/Gallery.tsx`, `scripts/build-photos.mjs`, `scripts/photos.manifest.json`
- [x] 1.3 Mark both as written after the fact, and carry known gaps as gaps

## 2. Backlog

- [x] 2.1 Retire the "shipped and NOT yet spec'd" note for `/gallery`
- [x] 2.2 Record that the month-long cycle gap is closed, and that `partners-page` needed no correction

## 3. Verification

- [x] 3.1 Every requirement traced to code that exists today, not to intent
- [x] 3.2 `npm run lint`, `npm test`, `npm run build` unaffected (documentation only)

## 4. Housekeeping

- [x] 4.1 Archive into `openspec/specs/`
